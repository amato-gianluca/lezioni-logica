\documentclass[aspectratio=169,10pt,dvipsnames,handout]{beamer}

%\setbeameroption{show notes}

\input{preamble.inc}

\usepackage[normalem]{ulem}

\title{Le forme proposizionali}

\begin{document}

\begin{frame}
    \titlepage
\end{frame}

\begin{frame}
    \copyrightpage
\end{frame}

\begin{frame}
    \bookpage{sulle sezioni 2.3, 3.1, 3.2, 4.1}
\end{frame}

\section{Forme proposizionali}

\begin{frame}{Forme proposizionali}
    Usando le lettere $A$, $B$, $C$ ecc., i connettivi $\neg$, $\land$, $\lor$, $\xor$, $\to$, $\iff$ e le parentesi tonde, è possibile costruire delle espressioni chiamate \alert{forme proposizionali} (abbreviate in \fp) o anche \alert{formule proposizionali}.
    \pause
    \begin{example}[Forme proposizionali]
        \centering
        $\neg(A \land \neg B)$ \qquad \qquad
        $(A \to B) \land (B \to A)$ \qquad \qquad
        $A \lor \neg (B \land C)$
    \end{example}
    \pause
    È possibile ottenere delle proposizioni rimpiazzando le lettere proposizionali:
    \begin{example}[dalle forme proposizionali alle proposizioni]
        Considerate la forma proposizionale $(A \lor B) \to C$. Allora se
        \begin{itemize}
            \item $A{}={}$ \prop{Carlo è laureato in economia}
            \item $B{}={}$ \prop{Carlo è laureato in informatica}
            \item $C{}={}$ \prop{Carlo è ammesso al concorso}
        \end{itemize}
        otteniamo
        \pause
        \begin{itemize}
            \item \prop{Se Carlo è laureato in economia o in informatica, allora è ammesso al concorso}.
        \end{itemize}
    \end{example}
\end{frame}

\begin{frame}{Calcolo del valore di verità di una forma proposizionale}
    Possiamo pensare alle forme proposizionali come espressioni algebriche che usano i valori vero e falso invece dei numeri. Se assegniamo un valore di verità alle lettere, è possibile calcolare il valore di verità della forma proposizionale.

    \begin{example}[Calcolo valore di verità]
        Consideriamo la forma proposizionale $(A \lor B) \to C$. Se
        \begin{itemize}
            \item $A={}$ vero
            \item $B={}$ falso
            \item $C={}$ vero
        \end{itemize}
        allora
        \begin{itemize}
            \item $(A \lor B) \to C = (\text{vero} \lor \text{falso}) \to \text{vero} = \pause \text{vero} \to \text{vero} = \pause \text{vero}$
        \end{itemize}
    \end{example}
\end{frame}

\begin{frame}{Parentesi e precedenze (1)}
    Le parentesi, come nelle espressioni algebriche, servono ad evitare le ambiguità:
    \begin{example}[Ambiguità]
        Consideriamo:
        \begin{itemize}
            \item $A \to B \land C$
            \item $A=\text{falso}$, $B=\text{vero}$ $C=\text{falso}$
        \end{itemize}.
        La \fp ha due interpretazioni, con due risultati diversi:
        \begin{itemize}
            \item $(A \to B) \land C  = (\text{falso} \to \text{vero}) \land \text{falso} = \pause \text{vero} \land \text{falso} = \pause \text{falso}$
            \item  \pause $A \to (B \land C)  = \text{falso} \to (\text{vero} \land \text{falso}) = \pause \text{falso} \to \text{falso} = \pause \text{vero}$
        \end{itemize}
    \end{example}
\end{frame}

\begin{frame}{Precedenze}
    Per evitare di mettere troppe parentesi, si usano le precedenze:
    \begin{itemize}
        \item $\neg$ ha la precedenza più alta
        \item $\land$, $\lor$ e $\xor$ hanno precedenza intermedia
        \item $\to$ e $\iff$ hanno precedenza più bassa
    \end{itemize}
    \pause
    \begin{example}[Precedenze]
        \begin{itemize}
            \item $A \to B \land C$ si legge \pause $A \to (B \land C)$
            \item $\neg A \land B \to C$ si legge \pause $((\neg A) \land B) \to C$.
        \end{itemize}
    \end{example}
    \pause
    \medskip
    Alcune parentesi sono comunque necessarie:
    \begin{itemize}
        \item $A \lor B \land C$ è ambigua, occorre scrivere $(A \lor B) \land C$ oppure $A \lor (B \land C)$.
    \end{itemize}

    \medskip
    Inoltre, spesso qualche parentesi in più non guasta:
    \begin{itemize}
        \item Invece di $\neg A \land B \to C$ meglio scrivere $(\neg A \land B) \to C$.
    \end{itemize}
\end{frame}

\begin{frame}{Tavole di verità per \fp (1)}
    Quando si calcola il valore verità di una \fp per tutti i possibili assegnamenti di valori di verità alle variabili proposizionali, si ottiene una \alert{tavola di verità}.

    \begin{example}[Tavola di verità di $\neg A \lor B$]
        \begin{center}
            \begin{tabular}{c|c||c}
                $A$ & $B$ & $\neg A \lor B$ \\
                \hline
                F   & F   & \only<2->V      \\
                F   & V   & \only<3->V      \\
                V   & F   & \only<4->F      \\
                V   & V   & \only<5->V
            \end{tabular}
        \end{center}
    \end{example}
\end{frame}

\tikzstyle{inline}=[remember picture, baseline, anchor=base, thick, inner sep=0, outer sep=0]


\begin{frame}{Tavole di verità per \fp (2)}

    Se la \fp è complessa, è possibile inserire eventuali colonne aggiuntive per formule intermedie.

    \begin{example}[Tavola di verità di $(A \to B) \to (\neg A \to B)$]

        \begin{center}
            \begin{tabular}{c|c||c|c|c|c}
                $A$ & \tikzmark{varb}$B$
                    & \tikz[inline]{\node (atob) {$A \to B$}}
                    & \tikz[inline]{\node (nega) {$\neg A$}}
                    & \tikz[inline]{\node (negatob1) {$\neg A$}} $\to$ \tikz[inline]{\node (negatob2) {$B$}}
                    & (\tikz[inline]{\node (final1) {$(A \to B)$}} $\to$
                \tikz[inline]{\node (final2) {$(\neg A \to B)$}}                                                                                                 \\
                \hline
                F   & F                                                                                      & \only<2->V & \only<3->V & \only<4->F & \only<5->F \\
                F   & V                                                                                      & \only<2->V & \only<3->V & \only<4->V & \only<5->V \\
                V   & F                                                                                      & \only<2->F & \only<3->F & \only<4->V & \only<5->V \\
                V   & V                                                                                      & \only<2->V & \only<3->F & \only<4->V & \only<5->V
            \end{tabular}
        \end{center}
        \onslide<4>{
            \begin{tikzpicture}[overlay, remember picture, thick]
                \node[fit=(negatob1), rectangle, rounded corners, draw, red, outer sep=0.2] {} ;
                \node[fit=(negatob2), rectangle, rounded corners, draw, red, outer sep=0.2] {} ;
                \draw[red] (nega) edge [->,bend left] (negatob1);
                \draw[red] (pic cs:varb) edge [->,bend right] (negatob2);
            \end{tikzpicture}
        }%
        \onslide<5>{
            \begin{tikzpicture}[overlay, remember picture, thick]
                \node[fit=(final1), rectangle, rounded corners, draw, red, outer sep=0.2] {} ;
                \node[fit=(final2), rectangle, rounded corners, draw, red, outer sep=0.2] {} ;
                \draw[red] (atob) edge [->,bend left] (final1);
                \draw[red] ($(negatob1)!0.5!(negatob2)$) edge [->,bend right] (final2);
            \end{tikzpicture}
        }%
    \end{example}
    \onslide<6->{Nella tabella di sopra ho voluto creare una colonna per ogni sottoformula, ma ovviamente ciò non è necessario}
\end{frame}

\begin{frame}{Tavole di verità per \fp (2)}
    È possibile disegnare la tavola di verità anche di \fp con tre o più lettere proposizionali: basta non sbagliare nel considerare tutte le possibili combinazioni di valori di verità.

    \begin{example}[Tavola di verità di $(A \to \neg C) \to (B \to \neg C)$]
        \begin{center}
            \begin{tabular}{c|c|c||c|c|c|c}
                $A$ & $B$ & $C$ & $\neg C$   & $A \to \neg C$ & $B \to \neg C$ & $(A \to \neg C) \to (B \to \neg C)$ \\
                \hline
                F   & F   & F   & \only<2->V & \only<3->V     & \only<4->V     & \only<5->V                          \\
                F   & F   & V   & \only<2->F & \only<3->V     & \only<4->V     & \only<5->V                          \\
                F   & V   & F   & \only<2->V & \only<3->V     & \only<4->V     & \only<5->V                          \\
                F   & V   & V   & \only<2->F & \only<3->V     & \only<4->F     & \only<5->F                          \\
                V   & F   & F   & \only<2->V & \only<3->V     & \only<4->V     & \only<5->V                          \\
                V   & F   & V   & \only<2->F & \only<3->F     & \only<4->V     & \only<5->V                          \\
                V   & V   & F   & \only<2->V & \only<3->V     & \only<4->V     & \only<5->V                          \\
                V   & V   & V   & \only<2->F & \only<3->F     & \only<4->F     & \only<5->V
            \end{tabular}
        \end{center}
    \end{example}
\end{frame}

\begin{frame}{Enumerare tutte le possibili combinazioni (1)}
    Come si fa a non dimenticarsi nessuna delle possibili combinazioni di valori di verità per le lettere proposizionali?
    \begin{itemize}
        \item con $n$ lettere proposizionali, ci sono $2^n$ combinazioni possibili;
        \item partendo dalla colonna più a destra, si alternano una ($2^0$) F e una V, fino ad arrivare a $2^n$ righe;
        \item quindi si passa alla colonna immediatamente a sinistra, alternando due ($2^1$) F e due V;
        \item quindi, passando alle colonne sempre più a sinistra, si alternano 4 ($2^2$), 8 ($2^3$), \ldots valori uguali (prima F poi V) fino a riempire tutte le colonne delle lettere proposizionali.
    \end{itemize}
\end{frame}

\begin{frame}{Enumerare tutte le possibili combinazioni (2)}
    \begin{center}
        \begin{columns}
            \column{0.5\textwidth}
            \small
            \centering
            \begin{tabular}{c|c|c|c}
                A          & B          & C          & D          \\
                \hline
                \only<5->F & \only<4->F & \only<3->F & \only<2->F \\
                \only<5->F & \only<4->F & \only<3->F & \only<2->V \\
                \only<5->F & \only<4->F & \only<3->V & \only<2->F \\
                \only<5->F & \only<4->F & \only<3->V & \only<2->V \\
                \only<5->F & \only<4->V & \only<3->F & \only<2->F \\
                \only<5->F & \only<4->V & \only<3->F & \only<2->V \\
                \only<5->F & \only<4->V & \only<3->V & \only<2->F \\
                \only<5->F & \only<4->V & \only<3->V & \only<2->V \\
                \only<5->V & \only<4->F & \only<3->F & \only<2->F \\
                \only<5->V & \only<4->F & \only<3->F & \only<2->V \\
                \only<5->V & \only<4->F & \only<3->V & \only<2->F \\
                \only<5->V & \only<4->F & \only<3->V & \only<2->V \\
                \only<5->V & \only<4->V & \only<3->F & \only<2->F \\
                \only<5->V & \only<4->V & \only<3->F & \only<2->V \\
                \only<5->V & \only<4->V & \only<3->V & \only<2->F \\
                \only<5->V & \only<4->V & \only<3->V & \only<2->V
            \end{tabular}
            \column{0.5\textwidth}
            \begin{itemize}
                \item<2-> colonna D: una F ed una V
                \item<3-> colonna C: due F e due V
                \item<4-> colonna B: quattro F e quattro V
                \item<5-> colonna A: otto F e otto V
            \end{itemize}
        \end{columns}
    \end{center}
\end{frame}

\begin{frame}{Enumerare tutte le possibili combinazioni (3)}
    Un metodo alternativo per non dimenticare nessuna combinazione è quello di riempiere le righe una alla volta, contando in binario, ma con F al posto di 0 e V al posto di 1.
    \begin{example}
        \begin{columns}
            \column{0.5\textwidth}
            \small
            \centering
            \begin{tabular}{c|c|c|c}
                A          & B          & C          & D          \\
                \hline
                \only<2->F & \only<2->F & \only<2->F & \only<2->F \\
                \only<3->F & \only<3->F & \only<3->F & \only<3->V \\
                \only<4->F & \only<4->F & \only<4->V & \only<4->F \\
                \multicolumn{4}{c}{\ldots}
            \end{tabular}
            \column[b]{0.5\textwidth}
            \begin{itemize}
                \item<2-> prima riga: $0 = 0000_2$
                \item<3-> seconda riga: $1 = 0001_2$
                \item<4-> terza riga: $2 = 0010_2$
            \end{itemize}
        \end{columns}
    \end{example}
\end{frame}

\begin{frame}{Esercizi consigliati}
    Esercizi da 16 a 26 del Capitolo 2.

    \medskip
    Esercizi da 1 a 4 del Capitolo 3.
\end{frame}

\note{Esercizi 2.20, 2.23, 2.26, 3.2}

\section{Tautologie, contraddizioni ed equivalenza logica}

\begin{frame}{Tautologie (1)}
    \begin{definition}[Tautologia]
        Si chiama \alert{tautologia} una forma proposizionale che è sempre vera, indipendentemente dal valore di verità delle lettere proposizionali che in essa compaiono.
    \end{definition}

    \begin{example}
        $A \land B \to A$ è una tautologia.
        \begin{center}
            \begin{tabular}{c|c||c|c|c}
                $A$ & $B$ & $A \land B$  & $(A \land B) \to A$ \\
                \hline
                F   & F   & \only<2->F & \only<3->V \\
                F   & V   & \only<2->F & \only<3->V \\
                V   & F   & \only<2->F & \only<3->V \\
                V   & V   & \only<2->V & \only<3->V
            \end{tabular}
        \end{center}
    \end{example}
\end{frame}

\begin{frame}{Tautologie (2)}
    Se una proposizione ha la forma di una tautologia sappiamo sicuramente che è vera, anche
    senza conoscere il valore di verità delle proposizioni semplici che la compongono.
    \begin{example}
        ``\prop{Se Carla è italiana e laureata, allora è italiana}\/'' è sicuramente una proposizione vera, anche se non sappiamo chi sia Carla.
    \end{example}

\end{frame}

\begin{frame}{Contraddizione}
    \begin{definition}[Contraddizione]
        Si chiama \alert{contraddizione} una forma proposizionale che è sempre falsa, indipendentemente dal valore di verità delle lettere proposizionali che in essa compaiono.
    \end{definition}
    \begin{example}
        $A \land \neg A$ è una contraddizione.
        \begin{center}
            \begin{tabular}{c||c|c}
                $A$ & $A \land \neg A$ \\
                \hline
                F   & \only<2->F  \\
                V   & \only<2->F  \\
            \end{tabular}
        \end{center}
    \end{example}
    \uncover<3->{
    Corrisponde a proposizioni in italiano che sono sicuramente false, come
    \begin{example}
        \prop{Carla è laureata ma non è laureata}.
    \end{example}}
\end{frame}


\begin{frame}{Tautologie e contraddizioni}
    Ovviamente, tautologie e contraddizioni sono strettamente correlate.

    \begin{theorem}
        $X$ è una tautologia se e solo se $\neg X$ è una contraddizione, e viceversa.
    \end{theorem}

    \begin{block}{Osservazione}
        In futuro, useremo:
        \begin{itemize}
            \item le lettere terminali dell'alfabeto come $X$, $Y$, $Z$ per indicare una \alert{forma proposizionale};
            \item le lettere iniziali dell'alfabeto come $A$, $B$, $C$ per le \alert{lettere proposizionali}.
        \end{itemize}
    \end{block}
\end{frame}

\begin{frame}{Tautologie notevoli: principio di non contraddizione}
    Il \alert{principio di non contraddizione} afferma che non è possibile che una proposizione sia contemporaneamente vera e falsa.

    \begin{theorem}[Principio di non contraddizione]
        $\neg (A \land \neg A)$ è una tautologia.
        \begin{center}
            \begin{tabular}{c||c|c}
                $A$ & $A \land \neg A$ & $\neg (A \land \neg A)$ \\
                \hline
                F   & \only<2->F        & \only<3->V               \\
                V   & \only<2->F        & \only<3->V               \\
            \end{tabular}
        \end{center}
    \end{theorem}
    \uncover<4->{
    \begin{example}[in italiano]
        ``\prop{Carla è laureata e non è laureata}\/'' è sicuramente falsa, anche se non sappiamo chi è Carla. Pertanto la sua negazione ``\prop{Non è vero che Carla è laureata e non è laureata}\/'' è sicuramente vera.
    \end{example}
    }
\end{frame}


\begin{frame}{Tautologie notevoli: terzo escluso}
    Il \alert{principio del terzo escluso}, noto anche col nome latino \emph{tertium non datur}, afferma sostanzialmente che ogni proposizione è vera o falsa.

    \begin{theorem}[Principio del terzo escluso]
        La \fp $A \lor \neg A$ è una tautologia.
        \begin{center}
            \begin{tabular}{c||c|c}
                $A$ & $A \lor \neg A$ \\
                \hline
                F   & \only<2->V      \\
                V   & \only<2->V      \\
            \end{tabular}
        \end{center}
    \end{theorem}
    \uncover<3->{
    \begin{example}[in italiano]
        ``\prop{Carla è laureata oppure non è laureata}'' è sicuramente vera, anche se non sappiamo chi è Carla.
    \end{example}}

    \medskip
    \onslide<4->{
        Il principio del terzo escluso, a differenza di quello di non contraddizione, \alert{non è universalmente accettato}: esistono molti sistemi logici in cui non vale, anche se noi non ce ne occupiamo.
    }
\end{frame}

\begin{frame}{Soddisfacibilità e falsificabilità}
    \begin{definition}[Soddisfacibilità]
        Una \fp si dice \alert{soddisfacibile} quando non è una contraddizione.
    \end{definition}

     In altri termini, una \fp è soddisfacibile se esiste almeno un'assegnazione di valori di verità alle lettere proposizionali (una riga della tavola di verità) che la rende vera.

    \medskip
    \begin{definition}[Falsificabilità]
        Una \fp si dice \alert{falsificabile} (o anche \alert{refutabile}) se non è una tautologia.
    \end{definition}

    In altri termini, una \fp è falsificabile se esiste almeno un'assegnazione di valori di verità alle lettere proposizionali (una riga della tavola di verità) che la rende falsa.
\end{frame}

\begin{frame}{Equivalenza logica}
    \begin{definition}[Equivalenza logica]
        Due fp si dicono \alert{logicamente equivalenti} se hanno lo stesso valore di verità per ogni possibile assegnamento di valori di verità alle lettere proposizionali.

        \medskip
        Detto in altri termini, due \fp sono equivalenti se hanno la stessa tavola di verità.
    \end{definition}

    \pause
    \begin{example}
        Consideriamo le \fp $\neg A \iff B$ e $A \iff \neg B$. Mostriamo un'unica tavola di verità che mostra entrambe le \fp assieme.
        \begin{center}
            \begin{tabular}{c|c||c|c|c|c}
                $A$ & $B$ & $\neg A$ & $\neg B$ & $\neg A \iff B$ & $A \iff \neg B$ \\
                \hline
                F   & F   & \only<3->{V        & V        & F               & F               }\\
                F   & V   & \only<3->{V        & F        & V               & V}               \\
                V   & F   & \only<3->{F        & V        & V               & V}               \\
                V   & V   & \only<3->{F        & F        & F               & F}
            \end{tabular}
        \end{center}
        \uncover<4->{
        Poiché le colonne $\neg A \iff B$ e $A \iff \neg B$ sono uguali, le due \fp sono logicamente equivalenti.
        }
    \end{example}
\end{frame}

\begin{frame}{Notazione per l'equivalenza logica}
    Nel seguito, useremo molto il concetto di equivalenza logica, per cui introduciamo il simbolo \alert{$\equiv$} per indicarla.

    \medskip
    Per dire che $\neg A \iff B$ e $A \iff \neg B$ sono equivalenti, scriveremo $\neg A \iff B \equiv A \iff \neg B$

    \begin{block}{Attenzione!}
        Il simbolo $\equiv$ \textbf{non è un connettivo} e $\neg A \iff B \equiv A \iff \neg B$ non è una forma proposizionale.  Il simbolo $\equiv$ è solo una notazione abbreviata per dire che due \fp sono equivalenti.
    \end{block}
\end{frame}

\begin{frame}{Tautologie ed equivalenza logica (1)}
    \begin{theorem}
        Due \fp $X$ e $Y$ sono logicamente equivalenti se e solo se la \fp $X \iff Y$ è una tautologia.
    \end{theorem}

    \begin{example}
        Le \fp $X = \neg A \lor B$ e $Y = A \to B$ sono equivalenti. Infatti
        \begin{center}
            \begin{tabular}{c|c||c|c|c}
                $A$ & $B$ & $\neg A$ & $\neg A \lor B$ & $A \to B$ \\
                \hline
                F   & F   & V        & V               & V         \\
                F   & V   & V        & V               & V         \\
                V   & F   & F        & F               & F         \\
                V   & V   & F        & V               & V
            \end{tabular}
        \end{center}
        Le due colonne $X = \neg A \lor B$ e $Y = A \to B$ sono uguali, quindi $X \equiv Y$.

        \smallskip Cosa possiamo dire di $X \iff Y$ ?
    \end{example}
\end{frame}

\begin{frame}{Tautologie ed equivalenza logica (2)}
    \begin{theorem}
        Due \fp $X$ e $Y$ sono logicamente equivalenti se e solo se la \fp $X \iff Y$ è una tautologia.
    \end{theorem}

    \begin{example}
        Aggiungiamo una nuova colonna alla tavola.
        \begin{center}
            \begin{tabular}{c|c||c|c|c|c}
                $A$ & $B$ & $\neg A$ & $\neg A \lor B$ & $A \to B$ & $(\neg A \lor B) \iff (A \to B)$ \\
                \hline
                F   & F   & V        & V               & V         & V                                \\
                F   & V   & V        & V               & V         & V                                \\
                V   & F   & F        & F               & F         & V                                \\
                V   & V   & F        & V               & V         & V
            \end{tabular}
        \end{center}
        La colonna $X \iff Y = (\neg A \lor B) \iff (A \to B)$ è sempre vera, perché i due valori di verità che la doppia implicazione collega sono uguali.

        \medskip Dunque $X \iff Y$ è una tautologia.
    \end{example}
\end{frame}

\begin{frame}{Esercizi consigliati}
    Esercizi 10 e 12 del Capitolo 2.

    \medskip
    Esercizi da 5 a 10 del Capitolo 3.
\end{frame}

\note{Esercizio 3.6}

\section{Equivalenze logiche notevoli}

\begin{frame}{Equivalenze logiche notevoli}
    L'equivalenza logica è assimilabile all'uguaglianza tra espressioni algebriche:
    \begin{itemize}
        \item siccome $x + y = y + x$, dovunque c'è una somma possiamo invertire gli addendi senza cambiare il risultato.
        \item allo stesso modo, siccome $A \lor B \equiv B \lor A$, possiamo invertire l'ordine delle proposizioni a cui si applica l'or senza cambiare il risultato (ovvero la tavola di verità)
    \end{itemize}

    % \medskip
    % Le equivalenze logiche ci danno quindi un modo di operare sulle \fp come fossero espressioni algebriche. Vediamo adesso alcune equivalenze notevoli, confrontandole con analoghe proprietà delle espressioni algebriche.

    \medskip
    Da ora in poi, \textbf{utilizzeremo nelle \fp anche i due simboli $\top$ e $\bot$}, che stanno per una formula sempre vera (tautologia) ed una sempre falsa (contraddizione). Non usiamo $V$ ed $F$ per evitare confusione con le lettere proposizionali.

    \medskip
    Vediamo quindi alcune equivalenze logiche notevoli. Quando possibile, faremo un parallelo con equivalenze notevoli delle espressioni algebriche, secondo la corrispondenza:
    \[
    \lor \Rightarrow + \qquad \land \Rightarrow \cdot \qquad \neg \Rightarrow 1 - {} \qquad \top \Rightarrow 1 \qquad \bot \Rightarrow 0
    \]
\end{frame}

\begin{frame}{Proprietà di congiunzione e disgiunzione}
    \centering
    \begin{tabular}{c|c|c}
        \textbf{logica}                                             & \textbf{nome proprietà} & \textbf{algebra}                               \\
        \hline
    $A \land B \equiv B \land A$                              & commutativa             & $x \cdot y = y \cdot x$                        \\
    $A \land (B \land C) \equiv (A \land B) \land C$        & associativa             & $x \cdot (y \cdot z) = (x \cdot y) \cdot z$    \\
    $A \land A \equiv A$                                       & idempotenza             & \sout{$x \cdot x = x$}                         \\
    $A \land \top \equiv A$                                    & elemento neutro         & $x \cdot 1 = x$                                \\
    $A \land \bot \equiv \bot$                                 & elem. assorbente        & $x \cdot 0 = 0$                                \\
        \hline
        \hline
    $A \lor B \equiv B \lor A$                                  & commutativa             & $x + y = y + x$                                \\
    $A \lor (B \lor C) \equiv (A \lor B) \lor C$                & associativa             & $x + (y + z) = (x + y) + z$                    \\
    $A \lor A \equiv A$                                         & idempotenza             & \sout{$x + x = x$}                             \\
    $A \lor \bot \equiv A$                                      & elemento neutro         & $x + 0 = x$                                    \\
    $A \lor \top \equiv \top$                                   & elem. assorbente        & \sout{$x + 1 = 1$}                             \\
        \hline
        \hline
    $A \land (B \lor C) \equiv (A \land B) \lor (A \land C)$ & distributiva            & $x \cdot (y + z) = x \cdot y + x \cdot z$      \\
    $A \lor (B \land C) \equiv (A \lor B) \land (A \lor C)$   & distributiva            & \sout{$x + y \cdot z = (x + y) \cdot (x + z)$} \\
    $A \land (A \lor B) \equiv A$                              & assorbimento            & \sout{$x \cdot (x + y) = x$}                   \\
    $A \lor (A \land B) \equiv A$                              & assorbimento            & \sout{$x + (x \cdot y) = x$}                   \\
    \end{tabular}
\end{frame}

\begin{frame}{Proprietà che coinvolgono la negazione}
    \begin{center}
        \begin{tabular}{c|c|c}
            \textbf{logica}               & \textbf{nome proprietà} & \textbf{algebra}             \\
            \hline
            $\neg \neg A \equiv A$      & doppia negazione        & $ 1 - (1 - A) = A$             \\
            $A \lor \neg A \equiv \top$   & terzo escluso           & $A + (1 - A) = 1$            \\
            $A \land \neg A \equiv \bot$ & non contraddizione      & \sout{$A \cdot (1 - A) = 0$} \\
            $\neg \top \equiv \bot$            &                         & $1 - 1 = 0$                  \\
            $\neg \bot \equiv \top$            &                         & $1- 0 = 1$
        \end{tabular}
    \end{center}
\end{frame}

\begin{frame}{Alcuni esempi in linguaggio naturale (1)}
    Il fatto che quelle viste prima siano tautologie segue dal calcolo delle tavole di verità. Cerchiamo comunque di dare una qualche intuizione nel linguaggio naturale.
    \begin{itemize}
    \item $A \land B \equiv B \land A$: cambiare l'ordine delle affermazioni, non cambia il significato.\\
        \prop{Carlo è alto e Mario è basso} $\equiv$ \prop{Mario è basso e Carlo è alto}.
    \item $A \land (B \land C) \equiv (A \land B) \land C$: non è facile fare un esempio in italiano, dove non ci sono le parentesi. Tuttavia, possiamo ripetere il soggetto in modo diverso per dare una idea del raggruppamento.\\
        \prop{Carlo è alto e biondo e Carlo è affabile} $\equiv$ \prop{Carlo è alto e Carlo è biondo e affabile}.
    \item $A \land A \equiv A$: ripetere due volte la stessa proposizione non ne cambia il significato.\\
        \prop{Carlo è alto e Carlo è alto} $\equiv$ \prop{Carlo è alto}.
    \item $A \land \top \equiv A$: \prop{Michele è o non è alto} è sicuramente vero, quindi non ha alcuna rilevanza.\\
        \prop{Carlo è alto e Michele è o non è alto} $\equiv$ \prop{Carlo è alto}.
    \item $A \land \bot \equiv \bot$: \prop{Michele è alto e non è alto} è sicuramente falso. Quindi\\
        \prop{Carlo è alto e Michele è alto e non è alto} è sicuramente falso.
    \end{itemize}
\end{frame}

\begin{frame}{Alcuni esempi in linguaggio naturale (2)}
    \begin{itemize}
    \item $A \land (B \lor C) \equiv (A \land B) \lor (A \land C)$:
        \prop{Carlo studia informatica e Carlo studia economia o filosofia} $\equiv$ \prop{Carlo studia informatica ed economia oppure Carlo studia informatica e filosofia}.
    \item $A \lor (B \land C) \equiv (A \lor B) \land (A \lor C)$:
        \prop{Carlo studia informatica oppure Carlo studia economia e filosofia} $\equiv$ \prop{Carlo studia informatica o economia e Carlo studia informatica o filosofia}. Tuttavia, l'equivalenza in italiano non è molto intuitiva.
    \item $A \land (A \lor B)$: \prop{Carlo studia informatica e Carlo studia informatica o economia} $\equiv$ \prop{Carlo studia informatica}. Perché la proposizione sia vera, è necessario che Carlo studi informatica, mentre che studi anche economia è ininfluente.
    \item $A \lor (A \land B)$: \prop{Carlo studia informatica oppure Carlo studia informatica ed economia} $\equiv$ \prop{Carlo studia informatica}. Come prima, perché la proposizione sia vera è sufficiente che Carlo studi informatica, mentre se studia anche economia è ininfluente.
        \item $\neg \neg A \equiv A$: due negazioni affermano.\\
        \prop{Non è vero che Carlo non studia informatica} $\equiv$ \prop{Carlo studia informatica}.
    \end{itemize}
\end{frame}

\begin{frame}{Equivalenza logica e insiemistica (1)}
    Vediamo quindi che per alcune equivalenze logiche vale una equivalenza algebrica corrispondente, ma non per tutte.

    \medskip
    In realtà, una totale corrispondenza si ha tra le equivalenze logiche e le equivalenze insiemistiche. Si consideri un insieme fissato $U$ (detto \alert{insieme universo}). Pensiamo alle lettere proposizionali come \alert{sottoinsiemi di $U$}. La corrispondenza tra logica e insiemistica è la seguente:
    \[
    \lor \Rightarrow \cup \qquad \land \Rightarrow \cap \qquad \neg \Rightarrow  U \setminus \qquad \top \Rightarrow U \qquad \bot \Rightarrow \emptyset
    \]
\end{frame}

\begin{frame}{Equivalenza logica e insiemistica (2)}
    \smallskip
    \centering
    \begin{tabular}{c|c|c}
        \textbf{logica}                                      & \textbf{nome}         & \textbf{insiemi}                          \\
        \hline
    $A \lor B \equiv B \lor A$                           & prop. commutativa     & $A \cup B = B \cup A$                     \\
    $A \lor (B \lor C) \equiv (A \lor B) \lor C$         & prop. associativa     & $A \cup (B \cup C) = (A \cup B) \cup C$   \\
    $A \lor A \equiv A$                                  & prop. di idempotenza  & $A \cup A = A$                            \\
    $A \lor \bot \equiv A$                               & elemento neutro       & $A \cup \emptyset = A$                    \\
    $A \lor \top \equiv \top$                            & elemento assorbente   & $A \cup U = U$                            \\
        \hline
        \hline
    $A \land B \equiv B \land A$                       & prop. commutativa     & $A \cap B = B \cap A$                     \\
    $A \land (B \land C) \equiv (A \land B) \land C$ & prop. associativa     & $A \cap (B \cap C) = (A \cap B) \cap C$ \\
    $A \land A \equiv A$                                & prop. di idempotenza  & $A \cap A = A$                            \\
    $A \land \top \equiv A$                             & elemento neutro       & $A \cap U = A$                            \\
    $A \land \bot \equiv \bot$                          & elemento assorbente   & $A \cap \emptyset = \emptyset$            \\
        \hline
        \hline
        $A \land (B \lor C)$                                & prop. distributiva    & $A \cap (B \cup C)$                       \\
        $A \lor (B \land C)$                                & prop. distributiva    & $A \cup (B \cap C)$                       \\
        $A \land (A \lor B) \equiv A$                       & prop. di assorbimento & $A \cap (A \cup B) =A$                    \\
        $A \lor (A \land B) \equiv A$                       & prop. di assorbimento & $A \cup (A \cap B) = A$                   \\
    \end{tabular}
\end{frame}

\begin{frame}{Equivalenza logica e insiemistica (3)}
    \begin{center}
        \begin{tabular}{c|c|c}
            \textbf{logica}               & \textbf{nome proprietà} & \textbf{insiemi}             \\
            \hline
            $\neg \neg A \equiv A$      & doppia negazione        & $ U \setminus (U \setminus A) = A$             \\
            $A \lor \neg A \equiv \top$   & terzo escluso           & $A \cup (U \setminus A) = U$            \\
            $A \land \neg A \equiv \bot$ & non contraddizione      & $A \cap (U \setminus A) = \emptyset$ \\
            $\neg \top \equiv \bot$            &                         & $U \setminus U = \emptyset$                  \\
            $\neg \bot \equiv \top$            &                         & $U \setminus \emptyset = U$
        \end{tabular}
    \end{center}
\end{frame}

\begin{frame}{Leggi di De Morgan (1)}
    Le leggi di De Morgan correlano congiunzione e disgiunzione con la negazione. Sono leggi molto importanti che consentono di semplificare le \fp complesse.
    \begin{gather*}
        \neg (A \land B) \equiv \neg A \lor \neg B\\
        \neg (A \lor B) \equiv \neg A \land \neg B
    \end{gather*}

    \begin{example}
        Consideriamo la proposizione:\\[0.1cm]
        \quad\prop{Carlo non studia informatica ed economia}\\[0.1cm]
        abbreviazione di\\[0.1cm]
        \quad\prop{Non è vero che Carlo studia informatica ed economia}\\[0.1cm]
        è equivalente a dire\\[0.1cm]
        \quad\prop{Carlo non studia informatica o Carlo non studia economia}
    \end{example}
\end{frame}

\begin{frame}[fragile]{Leggi di De Morgan (2)}
    \begin{example}
        Consideriamo un frammento di  programma in Python per calcolare le spese di spedizione.

        \begin{minted}[autogobble,xleftmargin=0.5cm]{python}
        shipping_charge = 10
        if not (state == "Italy" and region != "Sardegna"):
            shipping_charge = 20
    \end{minted}

        C'è una tariffa base di 10 €, ma sotto certe condizioni la tariffa sale a 20 €. Ma quali sono queste condizioni? Non si capisce molto bene.

        \pause \medskip
        Applicando De Morgan, possiamo riscrivere la condizione come:
        \begin{minted}[autogobble,xleftmargin=0.5cm]{python}
       (not state == "Italy") or (not regione != "Sardegna")
    \end{minted}

        \pause \medskip
        E quindi
        \begin{minted}[autogobble,xleftmargin=0.5cm]{python}
        state != "Italy" or regione == "Sardegna"
    \end{minted}
        Le spese aggiuntive si pagano se la spedizione è all'estero, oppure in Sardegna.
    \end{example}
\end{frame}

\begin{frame}{Il principio di dualità}
    \smallskip
    \begin{theorem}[Principio di dualità]
        Siano $X, Y$ due \fp tali che $X \equiv Y$, nessuna delle quali contiene $\to$. Se $X'$ e $Y'$ si ottengono da $X$ e $Y$ scambiando:
        \begin{itemize}
            \item $\land$ con $\lor$,
            \item $\leftrightarrow$ con $\xor$,
            \item $\top$ con $\bot$,
        \end{itemize}
        allora $X' \equiv Y'$.
    \end{theorem}

    \pause\medskip
    \begin{example}
        Prendiamo ad esempio l'equivalenza
        \[A \land \top \equiv A\]\pause
        Applicando ad ambo i membri dell'equivalenza gli scambi di prima, otteniamo
        \[A \lor \bot \equiv A\]
    \end{example}
\end{frame}

\begin{frame}{Proprietà associativa e parentesi}
    Il fatto che $\land$ e $\lor$ godano della proprietà associativa, ci consente di risparmiare qualche parentesi.
    \begin{itemize}
        \item<+-> Consideriamo la formula $A \land B \land C$.\\
        È \alert{ambigua}: la si può interpretare come $(A \land B) \land C$ oppure $A \land (B \land C)$.
        \item<+-> Le due possibili interpretazioni sono però \alert{equivalenti}, per la proprietà associativa, quindi l'ambiguità non ci crea nessun problema!
        \item<+-> La stessa cosa vale anche per $\lor$, ma solo se non c'è una mescolanza di $\land$ e $\lor$:
        \begin{itemize}
            \item \alert{possiamo scrivere $A \lor B \lor C$}\\[0.1cm]
            le due alternative $(A \lor B) \lor C$ e $A \lor (B \lor C)$ sono equivalenti;\\[0.2cm]
            \item \alert{non possiamo scrivere $A \land B \lor C$}\\[0.1cm]
             le due alternative $(A \land B) \lor C$ e $A \land (B \lor C)$ \textbf{non} sono equivalenti.
        \end{itemize}
    \end{itemize}
\end{frame}

\begin{frame}{Equivalenze con l'or esclusivo}
    La disgiunzione esclusiva è definibile a partire da congiunzione, disgiunzione (inclusiva) e negazione:
    \[
        A \xor B \equiv (A \land \neg B) \lor (\neg A \land B)
    \]
    Notare che le due \fp $A \land \neg B$ e $\neg A \land B$ corrispondono esattamente ai due casi in cui la disgiunzione esclusiva è vera (una delle due proposizioni è vera, l'altra è falsa).

    \pause \medskip Altre equivalenze notevoli:
    \begin{align*}
        A \xor B          & \equiv B \xor A          \\
        A \xor (B \xor C) & \equiv (A \xor B) \xor C \\
        A \xor \bot       & \equiv A                 \\
        A \xor \top       & \equiv \neg A            \\
        A \xor A          & \equiv \bot              \\
        A \xor \neg A     & \equiv \top              \\
    \end{align*}
\end{frame}

\begin{frame}{L'implicazione (1)}
    L'implicazione è definibile a partire da congiunzione, disgiunzione e negazione:
    \[
        A \to B \equiv \neg(A \land \neg B)  \equiv \neg A \lor B
    \]
    Detto in altro modo, affermare $A \to B$ è equivalente ad affermare:
    \begin{itemize}
        \item ``non è vero che $A$ è vero e contemporaneamente $B$ è falso''
        \item ``$A$ è falso oppure $B$ è vero''.
    \end{itemize}

    \smallskip
    Ancora:
    \begin{align*}
        A \to A      & \equiv \top   & (A \land B) \to C & \equiv A \to (B \to C)            \\
        A \to \neg A & \equiv \neg A & A \to (B \land C) & \equiv (A \to B) \land (A \to C) \\
        \bot \to A   & \equiv \top   & (A \lor B) \to C   & \equiv (A \to C) \land (B \to C) \\
        \top \to A   & \equiv A      & A \to (B \lor C)   & \equiv (A \to B) \lor (A \to C)   \\
        A \to \bot   & \equiv \neg A                                                          \\
        A \to \top   & \equiv \top
    \end{align*}
\end{frame}

\begin{frame}{L'implicazione (2)}
    Data un'implicazione $A \to B$, si possono definire altre tre implicazioni correlate:
    \begin{itemize}
        \item l'implicazione \alert{inversa}: $B \to A$
        \item l'implicazione \alert{contraria}: $\neg A \to \neg B$
        \item l'implicazione \alert{contronominale}: $\neg B \to \neg A$
    \end{itemize}
    L'inversa e la contraria non sono direttamente correlate con l'implicazione originale, invece la contronominale è equivalente all'originale:
    \[
        A \to B \equiv \neg B \to \neg A
    \]
    \begin{example}
        Data la proposizione ``\prop{se $x$ è divisibile per 4 allora x è pari}\/'',  vera per tutti gli $x$:
        \begin{itemize}
            \item l'inversa ``\prop{se $x$ è pari allora x è divisibile per 4}'' è falsa per $x=2$;
            \item la contraria ``\prop{se $x$ non è divisibile per 4 allora x non è pari}'' è falsa per $x=2$;
            \item la contronominale ``\prop{se $x$ non è pari allora x non è divisibile per 4}'' è vera per tutti i possibili $x$.
        \end{itemize}
    \end{example}
\end{frame}

\begin{frame}{Contronominale, modus ponens e modus tollens}
    Nella parte introduttiva del corso abbiamo visto due regole di inferenza che abbiamo chiamato \emph{modus ponens} e \emph{modus tollens}:

    \medskip
    \begin{center}
    \begin{inference}
        $A \to B$\\
        $A$\\
        \hline
        $B$
    \end{inference}
    (modus ponens)
    \hspace{2cm}
    \begin{inference}
        $A \to B$\\
        $\neg B$\\
        \hline
        $\neg A$
    \end{inference}
    (modus tollens)
    \end{center}
    In virtù della equivalenza logica tra $A \to B$ e $\neg B \to \neg A$, possiamo pensare al modus tollens come segue:
    \begin{center}
    \begin{inference}
        $\neg B \to \neg A$\\
        $\neg B$\\
        \hline
        $\neg A$
    \end{inference}
    \end{center}
    che quindi non è altro che il modus ponens ma applicato alla contronominale della proposizione $A \to B$.
\end{frame}

\begin{frame}{La doppia implicazione (1)}
    Due importanti equivalenze riguardanti la doppia implicazione sono
    \begin{gather*}
        A \iff B \equiv (A \to B) \land (B \to A)\\
        A \iff B \equiv (A \to B) \land (\neg A \to \neg B)
    \end{gather*}
    che mostrano come $A \iff B$ non è altro che la congiunzione di due implicazioni nei due versi opposti.

    \pause \medskip
    La doppia implicazione è inoltre definibile a partire dalla disgiunzione esclusiva (e viceversa).
    \begin{gather*}
        A \iff B \equiv \neg (A \xor B)\\
        A \xor B \equiv \neg (A \iff B)
    \end{gather*}
\end{frame}

\begin{frame}{La doppia implicazione (2)}
    La doppia implicazione è anche equivalente ad una formula che usa solo congiunzione, disgiunzione e negazione:

    \[
        A \iff B \equiv (A \land B) \lor (\neg A \land \neg B)
    \]
    dove $A \land B$ e $\neg A \land \neg B$ rappresentano i due casi per cui la doppia implicazione è vera.

    \pause\medskip
    Altre equivalenze che coinvolgono la doppia implicazione:
    \begin{align*}
        A \iff A      & \equiv \top     \\
        A \iff \neg A & \equiv \bot     \\
        A \iff \bot   & \equiv \neg A   \\
        A \iff \top   & \equiv A        \\
        A \iff B      & \equiv B \iff A \\
    \end{align*}
\end{frame}

\begin{frame}{Esercizi consigliati}
    Esercizi da 11 a 13 del Capitolo 3.

    \medskip
    Esercizi da 1 a 5 del Capitolo 4.
\end{frame}

\section{Semplificare le \fp}

\begin{frame}{Applicare una equivalenza ad una formula (1)}
    Il fatto che $A \land B \equiv B \land A$ vuol dire che, ovunque in una fp compare $A \land B$, è possibile rimpiazzarlo con $B \land A$ senza cambiare il significato della formula (e viceversa).
    \begin{itemize}
        \item $A \land B \to C \equiv$ \pause $B \land A \to C$ \pause\
        \item $(B \land A) \xor (C \iff A) \equiv$ \pause $(A \land B) \xor (C \iff A)$
    \end{itemize}

    \pause\medskip
    Analogamente, in matematica sappiamo che $x+y=y+x$. Questo ci consente di rimpiazzare $x+y$ con $y+x$ ovunque in una formula. Ad esempio,
    \begin{itemize}
        \item $(a(x+y))^2 = (a(y+x))^2$.
    \end{itemize}

    \pause\medskip
    Questo fatto si può formalizzare come segue:
    \begin{theorem}
        Siano $X$, $X'$ e $Y$ forme proposizionali, con $X \equiv X'$. Se $Y'$ è ottenuta da $Y$ rimpiazzando tutte le occorrenze di $X$ con $X'$ (o viceversa), allora $Y \equiv Y'$.
    \end{theorem}
\end{frame}

\begin{frame}{Applicare una equivalenza ad una formula (2)}
    Inoltre, in un'equivalenza come $A \land B \equiv B \land A$, le lettere $A$ e $B$ possono essere rimpiazzate con qualsiasi altra forma proposizionale, ottenendo sempre un'equivalenza:
    \begin{itemize}
        \item $C \land D \equiv$ \pause $D \land C$ \\
                (abbiamo rimpiazzato $A$ con $C$ e $B$ con $D$)
        \item $(A \to B) \land (A \lor C) \equiv$ \pause $(A \lor C) \land (A \to B)$\\
            (abbiamo rimpiazzato $A$ con $A \to B$, e $B$ con $A \lor C$)
    \end{itemize}


    \pause\medskip
    Analogamente, in matematica sappiamo che $x+y=y+x$. Questo ci consente di scambiare i due addendi in ogni espressione.
    \begin{itemize}
        \item $2xy^3 + \log z = \log z + 2xy^3$
    \end{itemize}

    \pause\medskip
    Questo fatto si può formalizzare come segue:
    \begin{theorem}
        Siano $X \equiv X'$. Se $Y$ e $Y'$ sono altre forme proposizionali ottenute da $X$ e $X'$ rimpiazzando le lettere proposizionali con forme proposizionali in maniera consistente (ovvero, la stessa lettera è sempre sostituita dalla stessa formula), allora $Y \equiv Y'$.
    \end{theorem}
\end{frame}

\begin{frame}{Applicare una equivalenza ad una formula (3)}
    In conclusione, il fatto che $A \land B \equiv B \land A$ vuol dire che:

    \smallskip
    \begin{quotation}
        \noindent in una forma proposizionale, ogni volta che compare il connettivo $\land$ è possibile cambiare l'ordine dei congiunti e ottenere una forma equivalente.
    \end{quotation}
    Analogamente per le altre equivalenze notevoli.

    \medskip \pause
    È possibile usare queste manipolazioni di tipo algebrico per
    \begin{itemize}
        \item verificare nuove equivalenze logiche in maniera più veloce, senza costruire la tavola di verità;
        \item semplificare delle forme proposizionali complesse.
    \end{itemize}
\end{frame}

\begin{frame}{Rimpiazzare una lettera con una formula (1)}
    Quando rimpiazziamo una lettera proposizionale con una fp, bisogna sempre valutare se aggiungere attorno ad essa delle parentesi, altrimenti rischiamo che la fp completa non si legga in modo corretto.
    \begin{example}
        Dato $A \land B \equiv B \land A$, se rimpiazziamo
        \begin{itemize}
            \item $A$ con $A \to B$,
            \item $B$ con $A \lor C$.
        \end{itemize}
        senza parentesi, otteniamo\\[0.1cm]
        \quad $A \to B \land A \lor C \equiv A \lor C \land A \to B$\\[0.1cm]
        che contiene fp ambigue per l'alternarsi di $\land$ e $\lor$. L'equivalenza corretta, che viene dalla originale proprietà commutativa, è:\\[0.1cm]
        \quad $(A \to B) \land (A \lor C) \equiv (A \lor C) \land (A \to B)$
    \end{example}
\end{frame}

\begin{frame}{Rimpiazzare una lettera con una formula (2)}
    Questo esempio è anche peggiore:
    \begin{example}
        Dato $A \land B \equiv B \land A$, se rimpiazziamo
        \begin{itemize}
            \item $A$ con $A$,
            \item $B$ con $B \to C$,
        \end{itemize}
        senza parentesi, otteniamo\\[0.1cm]
        \quad $A \land B \to C \equiv B \to C \land A$\\[0.1cm]
        che si legge\\[0.1cm]
        \quad $(A \land B) \to C \equiv B \to (C \land A)$\\[0.1cm]
        che non è un'equivalenza corretta! Quella corretta è:\\[0.1cm]
        \quad $A \land (B \to C) \equiv (B \to C) \land A$
    \end{example}
\end{frame}

\begin{frame}{Verificare una equivalenza logica (1)}
    Vogliamo verificare l'equivalenza $X \equiv Y$.  Si parte dalla proposizione sul lato sinistro o destro (quella che sembra più comoda) e si applicano le equivalenze note fino ad ottenere l'altra proposizione.

    \begin{example}
        Vogliamo verificare l'equivalenza $A \lor B \equiv \neg(\neg A \land \neg B)$. Partendo da destra:
        \[
            \begin{array}{rl@{\hspace{0.8cm}}l}
                              & \neg(\neg A \land \neg B) & \pause \text{(legge di De Morgan)} \\
                \pause \equiv & \neg\neg A \lor \neg\neg B & \pause \text{(doppia negazione)}   \\
                \pause \equiv & A \lor \neg\neg B          & \pause \text{(doppia negazione)}   \\
                \pause \equiv & A \lor B
            \end{array}
        \]
    \end{example}
\end{frame}

\begin{frame}{Verificare una equivalenza logica (2)}
    Spieghiamo più dettagliatamente il passaggio da $\neg(\neg A \land \neg B)$ a $\neg\neg A \lor \neg\neg B$.

    \begin{itemize}[<+->]
        \item Iniziamo a riscrivere la legge di De Morgan, ma per non fare confusione, usando le lettere $X$ e $Y$ invece di $A$ e $B$:
        \[
        \neg (X \land Y) \equiv \neg X \lor \neg Y
        \]
        \item Noi vogliamo applicare questa legge a $\neg(\neg A \land \neg B)$: dobbiamo capire se è possibile sostituire $X$ e $Y$ nella legge di De Morgan con delle formule, in maniera tale che
        $\neg (X \land Y)$ diventi $\neg(\neg A \land \neg B)$.
        \item Questo è possibile con la sostituzione $X \mapsto \neg A$ e $Y \mapsto \neg B$.
        \item Applicando queste sostituzioni alla legge di De Morgan, otteniamo
        \[\neg(\neg A \land \neg B) \equiv \neg\neg A \lor \neg\neg B \]
    \end{itemize}
    \pause
    \textbf{Procedimento analogo vale per l'applicazione delle altre equivalenze notevoli.}
\end{frame}

\begin{frame}{Semplificazione di una fp}
    Vogliamo semplificare la forma proposizionale $X$. La tecnica è la stessa usata sopra: si parte da $X$ e si applicano le equivalenze note fino ad ottenere una forma più semplice, ricordando che talvolta, per semplificare le cose, occorre prima complicarle!

    \begin{example}
        Vogliamo semplificare la fp $(A \land B) \lor (A \land \neg B)$.
        \[
            \begin{array}{rl@{\hspace{0.8cm}}l}
                              & (A \land B) \lor (A \land \neg B) & \pause \text{(prop. distributiva${}^*$)} \\
                \pause \equiv & A \land (B \lor \neg B)            & \pause \text{(terzo escluso)}            \\
                \pause \equiv & A \land \top                       & \pause \text{(elemento neutro di $\land$)}          \\
                \pause \equiv & A
            \end{array}
        \]
        Quello indicato con ${}^*$ è l'uso della proprietà distributiva $X \land (Y \lor Z) \equiv (X \land Y) \lor (X \land Z)$ ma da destra a sinistra. È l'operazione che nelle espressioni algebriche si chiama \emph{mettere in evidenza} o \emph{raccogliere}.
    \end{example}
\end{frame}

\note{Esercizio 4.5, $(A \to B) \land (A \to C) \equiv A \to (B \land C)$}

\end{document}
